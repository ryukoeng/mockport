# Distribution

Primary distribution paths:

| Path | Status |
| --- | --- |
| Docker image | Planned via GHCR workflow |
| Release binary archives | Planned via GitHub release workflow |
| Homebrew | Template only until release artifacts exist |
| npm | Experimental wrapper, not primary runtime |

Local release archive check:

```bash
scripts/test-release-archives.sh
```
