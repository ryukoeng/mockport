version: "0.1"

server:
  host: 127.0.0.1
  port: 43101

mode: ai-safe

adapters:
  stripe:
    enabled: true
    base_path: /stripe
    scenario: payment_success
    fake_secret: sk_live_example_should_not_be_used
    api_url: https://api.stripe.com
    webhook:
      target_url: http://app:3000/webhooks/stripe
      signing_secret: whsec_mockport
